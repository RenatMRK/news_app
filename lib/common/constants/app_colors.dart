import 'dart:ui';

abstract class AppColors {
  static const primaryBlue = Color(0xFF2F78FF); // основной цвет (seed)
  static const grey = Color(0xFFC1C1C1);        // вторичный оттенок / бордеры
  static const darkGrey = Color(0xFF6A6A6A);    // текст вторичный
  static const black = Color(0xFF000000);       // текст основной
  static const white = Color(0xFFFFFFFF);       // фон / onPrimary
}